{
  "name": "contracts",
  "version": "0.0.0",
  "private": true,
  "license": "none",
  "scripts": {
    "gen": "yarn artifacts",
    "artifacts": "mkdir -p ./artifacts/wallet-contracts && yarn artifacts-copy:wallet-contracts && yarn generate",
    "artifacts-copy:wallet-contracts": "rm -rf ./artifacts/wallet-contracts/* && cp -r ./node_modules/@0xsequence/wallet-contracts/artifacts/contracts/* ./artifacts/wallet-contracts",
    "generate": "go generate ./gen"
  },
  "devDependencies": {
    "@0xsequence/wallet-contracts": "1.8.0"
  }
}
